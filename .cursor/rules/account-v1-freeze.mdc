---
description: Profile (Main) hub — Owner Implementation Update; Profile-only redirects
globs: features/account-center/**/*,lib/account-center/canonical-menu.ts,lib/account/freeze.ts,app/account/**
alwaysApply: false
---

# Profile (Main) — Owner Implementation Update

**STATUS: PROFILE MAIN ONLY | ZERO DAMAGE | REDIRECTS ONLY**

## Protected

- Hub composition: Profile header → Menu → Sign Out
- Do not modify Homepage, Checkout, Orders logic, Wallet logic, Settings module, Payments, Shipping, DB, APIs, Auth

## Authorized menu (Profile only)

Favourites → `/saved` · Balance → `/wallet` · My Orders → `/orders` · Holiday Mode → `/seller` · Settings → `/settings` · Help → `/help` · Legal → `/legal` · Sign Out (confirm dialog)

## Header

Avatar 64px · username 16 Regular · View Profile purple · rating `5.0 ★ (n)` · full row clickable

## Reference

- Menu SSOT: `lib/account-center/canonical-menu.ts`
- Titles: `lib/account/freeze.ts` → `ACCOUNT_MENU_TITLES`
