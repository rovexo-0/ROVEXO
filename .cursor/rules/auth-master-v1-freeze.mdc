---
description: Auth Master Spec — Login-first startup, Splash/Welcome removed
alwaysApply: true
---

# ROVEXO v1.0 — AUTH MASTER SPEC (CANONICAL)

**STATUS: CERTIFIED | STARTUP SIMPLIFIED (PO PRIVATE DEPLOYMENT CONTRACT)**  
**Architecture freeze:** see `auth-master-freeze-v1.mdc` + `lib/auth/auth-master-freeze-v1.ts` (OWNER APPROVED · LOCKED · FROZEN).

**One authentication system only: Supabase Auth.** Do not create parallel auth, sessions, or guest gates.

## Canonical startup (locked)

App open → Authentication check →
- Valid verified session → **Homepage** immediately
- No session → **Login**
- Unverified session → **Verify email**

**Splash and Welcome are REMOVED.** Do not restore them.

## Canonical flow

Guest → Login → (Register → Email verify → Login) → Homepage
Logout → Login
Protected guest → Login (`next` preserved)

## Frozen UI

Login · Register remain production-locked presentation.

## Forbidden

Duplicate Login/Register · restoring Splash/Welcome · second auth system · custom session owners.
