---
description: Splash removed — Login is guest entry (PO private deployment contract)
alwaysApply: true
---

# ROVEXO v1.0 — SPLASH REMOVED

**STATUS: REMOVED | Product Owner private deployment contract**

Splash Screen is permanently removed from the canonical startup flow.

- Guest entry: `/login`
- Valid session: `/` (Homepage)
- `/splash` permanently redirects to `/login`
- PWA `start_url`: `/`

Do not restore Splash without a new approved Auth Master Specification.
