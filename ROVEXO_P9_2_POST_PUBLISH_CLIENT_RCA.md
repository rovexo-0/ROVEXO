# P9.2 — SELL POST-PUBLISH CLIENT RCA

**STATUS:** DIAGNOSTICS INSTALLED · **EXACT EXCEPTION PENDING OWNER CONSOLE CAPTURE** · **NO FIX**  
**Law:** Cod Sânge · Zero-risk diagnostics only · No publish/UI/CSS/API/DB/routing behaviour change · No commit / push / deploy  

---

## Proven (do not re-investigate)

| Fact | Status |
|---|---|
| `POST /api/listings` 200 | Proven |
| Listing written to DB | Proven |
| Listing on Homepage / feed | Proven |
| Upload / Publish pipeline / backend / Supabase / Feed | Proven · out of scope |

**Failure window:** after Publish SUCCESS → before / at FailClosed Error Boundary.

---

## Actual create post-publish chain (code — not the fictional checklist)

There is **no** `onPublishSuccess()` named callback, **no** `router.replace()`, **no** `invalidateQueries()`, **no** auto-close dialog in create path.

Instrumented **real** sequence in `SellProvider.publishListing` (create):

```
runPublishPipeline                          → FULFILLED  (= POST 200)
Publish SUCCESS
onPublishSuccess(create) ENTER
clearSellDraft
publishSuccessRef=true
createNewListingSession(clearPhotos+draft)  → async FULFILLED
resetDraftRefs
setDescriptionIsAutoGenerated(false)
setDraft(emptySession)
setRemovedImageIds([])
bumpPendingTextVersion
clearValidation(setShowValidation false)
setFormError(null)
setPublishSuccess(successPayload)
window.scrollTo(0,0)
router.refresh()
trackListingPublished
onPublishSuccess(create) EXIT
publishListing.finally
  → React remount/render
  → SellPageInner.successOpenEffect
  → setSuccessOpen(true)
  → PublishSuccessDialog MOUNT / open
```

Edit path separately logs `edit.router.push` + `edit.router.refresh`.

---

## Instrumentation installed (temporary · removable)

| Asset | Role |
|---|---|
| `lib/diagnostics/p92-post-publish-client-trace-v1.ts` | `[P9.2]` ENTER/EXIT/THROW/FULFILLED/REJECTED + timeline dump |
| `features/sell/diagnostics/use-p92-lifecycle.ts` | MOUNT / UNMOUNT / EFFECT_* |
| `features/sell/context/SellProvider.tsx` | wrap each post-success step; lifecycle on provider |
| `features/sell/ui/SellPage.tsx` | SellPage / Inner / overlay / formError hosts + successOpen effect |
| `components/sell/PublishSuccessDialog.tsx` | lifecycle + View Listing router wrap |
| `components/sell/PublishingOverlay.tsx` | lifecycle |
| `lib/diagnostics/failclosed-trace-p91b-v1.ts` | on FailClosed TRACE → `dumpP92ToConsole` + nav/fetch/rejection → `[P9.2]` |
| `FailClosedTraceBootstrap` | still installs listeners |

Console filter: **`[P9.2]`** and **`[FAILCLOSED_TRACE]`**

### Lifecycle components

`SellProvider` · `SellPage` · `SellPageInner` · `SellPublishingOverlayHost` · `SellFormErrorHost` · `PublishingOverlay` · `PublishSuccessDialog`

### FailClosed dump fields

On boundary TRACE, also logs:

- `lastStep` · `lastThrow` · `lastThrowStack`
- `lastSuccessfulCallback` · `lastSuccessfulPromise` · `lastRejectedPromise`
- `lastHook` · `lastMountedComponent`
- full `timeline` JSON (`TIMELINE_RAW`)

---

## How to capture (Owner)

1. Hard-refresh `http://localhost:3000/sell`
2. Console filter: `P9.2` (and `FAILCLOSED_TRACE`)
3. Publish until FailClosed
4. Copy **all** `[P9.2]` lines from `Publish SUCCESS` through last `THROW` / FailClosed `DUMP` / `TIMELINE_RAW`
5. Paste under § Capture results below (or send to Cursor)

The **last `[P9.2] THROW`** (or last timeline step before FailClosed DUMP) is the single instruction under test.

---

## Capture results

**STATUS: PENDING — not reproduced in this instrumentation session.**

### Timeline (fill from raw logs)

```
_pending Owner paste_
```

### Root Cause

| Field | Value |
|---|---|
| Exact file | _pending_ |
| Exact line | _pending_ |
| Exact component | _pending_ |
| Exact hook | _pending_ |
| Exact callback | _pending_ |
| Exact Promise | _pending_ |
| Exact stack | _pending_ |

### Classification (one)

_pending_ — one of: React exception · Provider exception · useEffect exception · Router refresh exception · Client component exception · Promise rejection · Other

### Evidence (raw logs)

```
_pending Owner paste — no summaries only_
```

---

## Classification guide (after capture)

| Last signal before FailClosed | Likely class |
|---|---|
| `THROW` on `router.refresh()` | Router refresh exception |
| `THROW` on `createNewListingSession…` | Promise / draft-clear exception |
| `THROW` on `setPublishSuccess` / draft setters | React/state exception |
| MOUNT `PublishSuccessDialog` then THROW / FailClosed digest | Client component / render exception |
| `successOpenEffect` then FailClosed with digest, no client THROW | Router refresh → Server Component → boundary |
| `promise.unhandledrejection` | Promise rejection |
| `catch_AFTER_SUCCESS` | Sync throw inside success chain (caught by publish try/catch — may show formError not FailClosed) |

---

## Removal checklist (after RCA · Owner-authorized)

1. Delete `lib/diagnostics/p92-post-publish-client-trace-v1.ts`  
2. Delete `features/sell/diagnostics/use-p92-lifecycle.ts`  
3. Revert SellProvider / SellPage / PublishSuccessDialog / PublishingOverlay P9.2 wraps  
4. Revert FailClosed P9.2 dump / nav p92Log hooks (keep or remove P9.1B per Owner)  
5. Ripgrep zero: `[P9.2]` · `p92-post-publish` · `useP92Lifecycle`

---

## Strict compliance

- NO fixes · NO refactor · NO optimisation · NO UI/CSS · NO DB · NO API · NO routing behaviour change  
- NO commit · NO push · NO deploy  
- Stop after exact instruction identified from logs  

**This report cannot name the exact line until `[P9.2]` THROW / FailClosed DUMP is captured.**
