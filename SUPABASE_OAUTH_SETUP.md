# SUPABASE OAUTH SETUP (GOOGLE + APPLE)

**STATUS:** OWNER READY TO CONFIGURE  
**Project ref (from `NEXT_PUBLIC_SUPABASE_URL`):** `pklotmwxtnnepaitedic`  
**Project URL:** `https://pklotmwxtnnepaitedic.supabase.co`  

**Application frozen.** Configure Dashboard only. Never invent Client IDs or Secrets.

---

## 1. Open the correct project

1. https://supabase.com/dashboard  
2. Select project whose API URL is:

```text
https://pklotmwxtnnepaitedic.supabase.co
```

---

## 2. URL Configuration (do this once for both providers)

**Authentication** → **URL Configuration**

### Site URL — paste exactly

```text
https://www.rovexo.co.uk
```

### Additional Redirect URLs — paste exactly (one per line)

```text
https://www.rovexo.co.uk/auth/callback
http://localhost:3000/auth/callback
```

Optional (only if staging host is in use for this project):

```text
https://staging.rovexo.com/auth/callback
```

Click **Save**.

### Callback map (do not invent)

| Name | Exact value |
|------|-------------|
| Supabase OAuth callback (Google Redirect URI + Apple Return URL) | `https://pklotmwxtnnepaitedic.supabase.co/auth/v1/callback` |
| Production ROVEXO callback | `https://www.rovexo.co.uk/auth/callback` |
| Local ROVEXO callback | `http://localhost:3000/auth/callback` |

---

## 3. Enable Google

**Authentication** → **Providers** → **Google**

| Field | Action |
|-------|--------|
| Enable Sign in with Google | **ON** |
| Client ID | Paste from Google Cloud OAuth Web client (`GOOGLE_OAUTH_OWNER_SETUP.md`) |
| Client Secret | Paste from Google Cloud OAuth Web client |

**Save.**

Google Cloud must already list Redirect URI:

```text
https://pklotmwxtnnepaitedic.supabase.co/auth/v1/callback
```

and JS origins:

```text
https://www.rovexo.co.uk
http://localhost:3000
```

---

## 4. Enable Apple

**Authentication** → **Providers** → **Apple**

| Field | Action |
|-------|--------|
| Enable Sign in with Apple | **ON** |
| Client IDs | Paste Apple **Services ID** |
| Secret Key | Paste full `.p8` private key contents |
| Key ID | Paste Apple Key ID |
| Team ID | Paste Apple Team ID |

**Save.**

Apple Services ID must already use:

| Field | Exact value |
|-------|-------------|
| Domains | `pklotmwxtnnepaitedic.supabase.co` |
| Return URLs | `https://pklotmwxtnnepaitedic.supabase.co/auth/v1/callback` |

Client secret JWT is **generated by Supabase** from the `.p8` (see `APPLE_OAUTH_OWNER_SETUP.md` STEP 3).

---

## 5. Facebook

**Do not enable** for public Login/Register unless Owner changes RC1 policy (Facebook is public-forbidden). Email + Google + Apple only.

---

## 6. Recommended Auth settings

| Setting | Owner action |
|---------|----------------|
| Automatic linking / same email | Enable so Google/Apple do not create a second user for an existing verified email |
| Email provider | Keep **enabled** (already live) |

---

## 7. Environment variables (Vercel Production)

OAuth **Client ID / Secret are not** stored in ROVEXO app env for this stack.

Confirm Production has:

| Variable | Expected production value |
|----------|---------------------------|
| `NEXT_PUBLIC_SUPABASE_URL` | `https://pklotmwxtnnepaitedic.supabase.co` |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | Production anon / publishable key (Owner-managed) |
| `SUPABASE_SERVICE_ROLE_KEY` | Production service role (Owner-managed) |
| `NEXT_PUBLIC_SITE_URL` | `https://www.rovexo.co.uk` |
| `NEXT_PUBLIC_APP_URL` | `https://www.rovexo.co.uk` (if used) |

Optional (do not force true before providers work):

| Variable | Purpose |
|----------|---------|
| `OAUTH_GOOGLE_ENABLED` | UI probe override |
| `OAUTH_APPLE_ENABLED` | UI probe override |

---

## 8. Smoke probe after Save

```bash
curl -s "https://pklotmwxtnnepaitedic.supabase.co/auth/v1/settings"
```

Expect in JSON `external`:

- `"google": true`
- `"apple": true`
- `"email": true`

```bash
curl -sI "https://pklotmwxtnnepaitedic.supabase.co/auth/v1/authorize?provider=google&redirect_to=https%3A%2F%2Fwww.rovexo.co.uk%2Fauth%2Fcallback"
curl -sI "https://pklotmwxtnnepaitedic.supabase.co/auth/v1/authorize?provider=apple&redirect_to=https%3A%2F%2Fwww.rovexo.co.uk%2Fauth%2Fcallback"
```

Expect **HTTP 302/303** with `Location` to Google / Apple — **not** HTTP 400 `provider is not enabled`.

Then complete `OAUTH_VERIFICATION_CHECKLIST.md` on devices.

---

**No code. No commit. No push. No Preview. No Production deploy.**
