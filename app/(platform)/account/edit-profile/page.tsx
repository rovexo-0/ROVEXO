import { redirect } from "next/navigation";

/** My Profile v8.0 — Edit Profile entry (SSOT → Personal Information). */
export default function AccountEditProfileRoute() {
  redirect("/account/profile");
}
