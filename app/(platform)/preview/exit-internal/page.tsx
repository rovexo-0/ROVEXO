import { Run4InternalPreviewHub } from "@/components/preview/Run4InternalPreviewHub";

/** TEMPORARY RUN #4 — clear internal pad preview. */
export default function PreviewExitInternalPage() {
  return <Run4InternalPreviewHub pad={24} mode="exit" />;
}
