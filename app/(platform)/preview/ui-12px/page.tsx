import { Run3PreviewHub } from "@/components/preview/Run3PreviewHub";

/** TEMPORARY RUN #3 — localhost Design Review entry (12px). Not SSOT. */
export default function PreviewUi12Page() {
  return <Run3PreviewHub pad={12} mode="enter" />;
}
