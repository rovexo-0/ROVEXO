import { Run3PreviewHub } from "@/components/preview/Run3PreviewHub";

/** TEMPORARY RUN #3 — localhost Design Review entry (24px). Not SSOT. */
export default function PreviewUi24Page() {
  return <Run3PreviewHub pad={24} mode="enter" />;
}
