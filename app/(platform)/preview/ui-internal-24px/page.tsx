import { Run4InternalPreviewHub } from "@/components/preview/Run4InternalPreviewHub";

/** TEMPORARY RUN #4 — Internal UI before (24px). Homepage locked. SSOT unchanged. */
export default function PreviewInternal24Page() {
  return <Run4InternalPreviewHub pad={24} mode="enter" />;
}
