import { Run3PreviewHub } from "@/components/preview/Run3PreviewHub";

/** TEMPORARY RUN #3 — clear localhost pad preview overlay. */
export default function PreviewExitPage() {
  return <Run3PreviewHub pad={24} mode="exit" />;
}
