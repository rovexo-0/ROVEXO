import { Run4InternalPreviewHub } from "@/components/preview/Run4InternalPreviewHub";

/** TEMPORARY RUN #4 — Internal UI after (16px). Homepage locked. SSOT unchanged. */
export default function PreviewInternal16Page() {
  return <Run4InternalPreviewHub pad={16} mode="enter" />;
}
