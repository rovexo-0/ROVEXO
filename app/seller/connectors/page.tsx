import { redirect } from "next/navigation";

/** Marketplace Connectors / Import removed (PO Final Authorization). */
export default function SellerMarketplaceConnectorsRemoved() {
  redirect("/seller");
}
