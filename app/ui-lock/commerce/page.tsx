import { redirect } from "next/navigation";

/** Absolute Final: UI-lock preview removed — one product, Master Image Pack only. */
export default function CommerceUiLockPreviewPage() {
  redirect("/");
}
