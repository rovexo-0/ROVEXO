# ROVEXO v1.0 — SPRING 2 TRANSACTION HUB ENGINE  
# Canonical Code Blood v2.0 (LOCKED)

| Field | Value |
|---|---|
| **Blood** | CANONICAL CODE BLOOD **v2.0 LOCKED** |
| **Status** | WAITING FOR IMPLEMENTATION |
| **Host** | `http://localhost:3000` only |
| **SSOT** | `lib/inbox/transaction-hub-spring-2-v1.ts` |

## Golden rule

```
IF I RECEIVE IT → I MUST SEE IT
IF I SEE IT → I MUST OPEN IT
IF I OPEN IT → IT MUST DO SOMETHING
IF I CANNOT SEE IT → IT DOES NOT EXIST
IF IT DOES NOT WORK → IT DOES NOT EXIST
```

## Certification (only)

```
localhost:3000 → Owner clicks → sees → opens → uses → approves → FREEZE → LOCK → NEXT
```

Forbidden alone: HTTP 200 · API · DB · console · unit · Playwright · screenshot without Owner visual.

## Mission

EVENT → RED BADGE → NOTIFICATION → MESSAGE → PRODUCT IMAGE → READ/UNREAD → CLICK → REAL PAGE → REAL ACTION → VISUAL PROOF → FREEZE

## Laws (v2.0)

| Law | Rule |
|-----|------|
| **Click path** | Every click opens a real page (Offer / Order / Tracking / Refund / Conversation). Text-only = FAIL |
| **Product image** | Required on messages, notifications, orders, payments, shipping — no image = NO PASS |
| **Read/Unread** | Unread → badge → click → read → badge updates → none left → badge gone |
| **No duplicates** | One event = one notification = one click = one page |
| **Badge realtime** | 0 / +1 / +10 / 99+ — must update in real time |
| **Absolute UX** | No empty pages · dead buttons/links/notifications · text-only events · parallel systems · click without action · badge without event · event without click path |

## Visual proofs

1. Offer → +1 → image/price/time → Offer page → read → 0  
2. Message → +1 → image/last/time → Conversation → read → 0  
3. Payment → +1 → image/order/price → Order page → read → 0  
4. Shipped → +1 → tracking/image → Tracking page → read → 0  

## Final

EVENT → badge → Owner sees → click → notification + image → click → real page → read → badge updates → Owner uses → Owner approves → PASS → FREEZE → LOCK → CERTIFIED → NEXT SPRING

## Status

**BLOOD v2.0 LOCKED · WAITING FOR IMPLEMENTATION.**  
Say **START SPRING 2** to begin.
