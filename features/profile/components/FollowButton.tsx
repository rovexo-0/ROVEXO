/**
 * Canonical FollowButton — re-export only (Phase 3 SSOT).
 * Implementation lives in components/follow/FollowButton.tsx.
 */
export {
  FollowButton,
  type FollowCounts,
} from "@/components/follow/FollowButton";
