/**
 * Sell layout tokens — Account CDS only.
 * No Sell-only cards, inputs, or shadows.
 */

export { focusRing } from "@/components/ui/tokens";

/** Fullscreen picker shell — white surface under Account header. */
export const sellPanel = "flex min-h-0 flex-1 flex-col bg-white";
