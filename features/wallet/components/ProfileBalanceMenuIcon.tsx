"use client";

/**
 * Balance module icon — inherits Profile Icon System v1.0 (wallet · #06B6D4 · 24px).
 * One source: ProfileMenuIcon / PROFILE_BALANCE_ICON.
 */

import { ProfileMenuIcon } from "@/features/account-center/components/ProfileMenuIcons";

export function ProfileBalanceMenuIcon() {
  return <ProfileMenuIcon id="balance" />;
}
