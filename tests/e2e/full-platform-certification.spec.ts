/**
 * Pointer — Playwright testDir is `./e2e` (see playwright.config.ts).
 * Canonical Full Platform Certification E2E:
 *   e2e/full-platform-certification.spec.ts
 *
 * Blood Law XLII — run against http://localhost:3000 only.
 */
export {};
